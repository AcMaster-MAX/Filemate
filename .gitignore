# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
*.egg

# ── 虚拟环境 ──
.venv/
venv/
.env
.env.local

# ── 编辑器 / OS ──
.vscode/
.idea/
*.swp
*.swo
Thumbs.db
Desktop.ini

# ── 项目数据（不入库）──
filemate.db
filemate.db-journal
datasets/raw/*
datasets/long_text/*
!datasets/raw/.gitkeep
!datasets/long_text/.gitkeep

# ── OCR 模型缓存 ──
.paddleocr/
__paddle__/

# ── 日志 / 输出 ──
logs/
*.log
*.ics
# 测试结果 JSON 不入库（分类 / 实体 / 里程碑 / 命名四份 + W4 联调）
test_*result*.json
*_result.json
filemate.db*
