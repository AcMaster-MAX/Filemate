# 命名生成规范 v1

## 命名模板
```
[课程名]-[文件类型]-[任务描述]-[截止时间]-[状态]
```

## 各字段说明

| 字段 | 说明 | 示例 |
|---|---|---|
| 课程名 | 文件所属的课程名称 | "操作系统"、"高等数学" |
| 文件类型 | 见分类体系 | "课件"、"作业"、"竞赛通知" |
| 任务描述 | 文件的核心内容简述，≤15字 | "实验三"、"第三章习题" |
| 截止时间 | 有日期时转为 MMDD 格式，无截止日期填"待定" | "0415"（表示4月15日） |
| 状态 | 文件当前状态 | "待处理" |

## 完整示例

```
[操作系统]-[作业]-[实验三]-[待定]-[待处理]
[高等数学]-[参考资料]-[第三章]-[待定]-[待处理]
[数学建模]-[竞赛通知]-[报名截止]-[0415]-[待处理]
[数据结构]-[考试通知]-[期末考试]-[0620]-[待处理]
[大学生创新创业]-[大创通知]-[立项申报]-[待定]-[待处理]
[综合测评]-[待确认]-[第二课堂成绩统计]-[0816]-[待处理]
```

## 规则

1. 文件名总长度不超过 80 字符（不含扩展名）
2. 课程名过长时截断为前 10 个字
3. 任务描述超过 15 字时，调用 LLM 精简到 15 字以内；LLM 失败则硬截断到 15 字
4. 截止日期统一为 MMDD 格式（如 0415 表示 4 月 15 日），无截止日期填"待定"
5. 不包含任何非法字符（\ / : * ? " < > |）
6. 如果某个字段无法提取，使用默认值：课程名="未分类"，任务="未命名"，截止="待定"

## 实现方式
命名生成器以规则拼接为主，task 超过 15 字时调用 LLM 精简。LLM 失败则硬截断。
